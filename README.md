# Portainer + Traefik Produção (Docker Swarm)

Instalação automática com **uma única linha**:

```bash
sudo apt update && sudo apt install -y git && git clone https://github.com/lucasolasz/portainerprod-autoinstall.git && cd portainerprod-autoinstall && sudo chmod +x install.sh && ./install.sh